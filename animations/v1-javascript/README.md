# Animations - v1 JavaScript

Make the GA logo move back and forth across the screen
using native (vanilla) JavaScript.

## Prerequisites

* `document.getElementById()`
* `window.parseInt()`
* `window.getComputedStyle()`
* `element.style`
* `if () { ... }`
* `window.setInterval()`
